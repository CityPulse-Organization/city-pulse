name: auth-service CI/CD

on:
  push:
    paths:
      - 'backend/auth-service/**'
      - 'backend/city-pulse-common/**'
      - 'backend/pom.xml'
  workflow_dispatch:

jobs:
  call-common-ci:
    uses: ./.github/workflows/common-service-ci.yml
    with:
      service_name: 'auth-service'
      service_path: 'backend/auth-service'
    secrets: inherit
