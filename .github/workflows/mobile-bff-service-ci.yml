name: mobile-bff-service CI/CD

on:
  push:
    paths:
      - 'backend/mobile-bff-service/**'
      - 'backend/city-pulse-common/**'
      - 'backend/pom.xml'
      - '.github/workflows/mobile-bff-service-ci.yml'
      - '.github/workflows/common-service-ci.yml'
  release:
    types: [published]
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  call-common-ci:
    uses: ./.github/workflows/common-service-ci.yml
    with:
      service_name: 'mobile-bff-service'
      service_path: 'backend/mobile-bff-service'
